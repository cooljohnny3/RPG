#include "../src/headers/Player.h"
#include "gtest/gtest.h"

//Player emptyPlayer = Player();
// TODO look at Player and Creature and fugire out why player has all ints for constructor
//Player constructedPlayer = Player("Name", );

TEST(PlayerTest, getName) {
  EXPECT_EQ("", "");
  //EXPECT_EQ();
}

TEST(PlayerTest, getMaxHealth) {
  EXPECT_EQ(100, 100);
  //EXPECT_EQ();
}

TEST(PlayerTest, getHealth) {
  EXPECT_EQ(100, 100);
  //EXPECT_EQ();
}